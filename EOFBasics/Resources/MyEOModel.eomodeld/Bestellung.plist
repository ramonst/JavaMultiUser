{
    attributes = (
        {columnName = DATUM; name = datum; prototypeName = dateTime; }, 
        {allowsNull = N; name = id; prototypeName = id; }, 
        {allowsNull = N; columnName = kundeID; name = kundeID; prototypeName = id; }
    ); 
    attributesUsedForLocking = (datum, id, kundeID); 
    className = "ch.wiss.EOBasics.eo.Bestellung"; 
    classProperties = (bestellpositionen, datum, kunde); 
    externalName = BESTELLUNG; 
    fetchSpecificationDictionary = {}; 
    name = Bestellung; 
    primaryKeyAttributes = (id); 
    relationships = (
        {
            destination = Bestellposition; 
            isToMany = Y; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = bestellungID; sourceAttribute = id; }); 
            name = bestellpositionen; 
        }, 
        {
            destination = Kunde; 
            isMandatory = Y; 
            isToMany = N; 
            joinSemantic = EOInnerJoin; 
            joins = ({destinationAttribute = id; sourceAttribute = kundeID; }); 
            name = kunde; 
        }
    ); 
}
